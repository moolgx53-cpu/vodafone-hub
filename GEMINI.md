# قواعد وتوجيهات مشروع Vodafone Egypt Hub

## 🔄 التحديث التلقائي الإلزامي بعد كل تعديل (Mandatory Auto-Versioning Rule)

في كل مرة يطلب فيها المستخدم أي تعديل أو إضافة أو حذف باقة أو كود أو تحسين في الأداة:
1. **تطبيق التعديل المطلوب**: في ملفات المشروع (مثل `index.html`).
2. **تحديث رقم الإصدار وتاريخ التعديل تلقائياً**:
   - في `version.json`:
     - زيادة رقم الإصدار (مثال: من `1.0.1` إلى `1.0.2`).
     - تحديث تاريخ اليوم في `updatedAt` بصيغة `YYYY-MM-DD`.
     - كتابة ملخص موجز باللغة العربية للتعديل في حقل `changelog`.
   - في `sw.js`:
     - تحديث قيمة `CACHE_VERSION` بنفس رقم الإصدار الجديد (مثال: `'v1.0.2'`).
   - في `index.html`:
     - تحديث ثابت `CURRENT_APP_VERSION` بنفس رقم الإصدار الجديد.
     - تحديث النص الافتراضي في `#txt-version-number` ليطابق الإصدار الجديد.
3. **النشر التلقائي عبر Git**:
   - تنفيذ أوامر الرفع فوراً:
     ```bash
     git add .
     git commit -m "تحديث تلقائي: [وصف التعديل] (vX.X.X)"
     git push origin main
     ```
   - التأكد من إتمام الرفع لكي يتم تحديث موقع GitHub Pages وكافة أجهزة المستخدمين تلقائياً.
